[package]
name = "arty_a7"
version = "0.1.0"
edition = "2024"

[dependencies]
riscv-rt = { version = "0.18", features = ["single-hart"] }
libm = "0.2"
log = "0.4"
riscv = "0.16"

noelv = { path = "../noelv" }

[package]
name = "grlib"
version = "0.1.0"
edition = "2024"

[dependencies]
derive-mmio = "0.8"
bitbybit = "2"
arbitrary-int = "2"
embedded-hal = "1"
embedded-io = "0.7"
static_assertions = "1"

defmt = { version = "1", optional = true }
